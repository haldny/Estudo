<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.haldny.estudo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.home.view.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.game.view.GameActivity" />
        <activity android:name=".mvp.gamedatapersisted.view.GameActivity" />
        <activity android:name=".gamegod.GameActivity" />
        <activity android:name=".mvvm.archcomponent.view.GameActivity" />
        <activity android:name=".mvvm.databinding.view.GameActivity" />
    </application>

</manifest>